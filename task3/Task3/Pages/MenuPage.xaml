<Page x:Class="Task3.Pages.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Task3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Menu">

    <Grid>
        <TextBlock Text="HI FROM MENU" FontSize="30"/>

        <!--<ListBox  Name="Items" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox VerticalContentAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="10"  Text = "{Binding Name}"></TextBlock>
                            <TextBlock Margin="10" Text = "{Binding Qty}"></TextBlock>
                            <TextBlock Margin="10" Text = "{Binding Price}"></TextBlock>
                            <TextBlock Margin="10" Text = "{Binding Sum}"></TextBlock>
                            <Image Source="{Binding ImgPath}" Height="50" Width="50"/>
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
    </Grid>
</Page>
